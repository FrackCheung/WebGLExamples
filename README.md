#### WebGL案例

使用`npm`安装`anywhere`, 进入`docs`目录, 运行`anywhere`命令
```bash
npm install anywhere
cd docs
anywhere

# 或者anywhere -p 端口号
# 更多命令请自行探索
```

推荐将`anywhere`进行全局安装, 这样在任意目录下, 都可以运行`anywhere`命令以服务形式访问
```
npm install anywhere -g
```
